# Hooks Directory

This directory contains custom React hooks for the Blockscout dashboard.

- Use this directory for reusable logic that can be shared across components.
- Example: `useExample.ts` provides a simple greeting message.

## Usage

Import hooks from this directory into your components as needed. 